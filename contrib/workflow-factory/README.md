# workflow-factory (resgate de source)

Source do pacote `workflow-factory` 0.1.0 ("Idempotent GitHub-to-Dokploy project factory for Hermes"), reconstruído em 28/08/2026 a partir do site-packages de `/opt/workflow-factory/venv` na VPS: o pacote estava instalado em produção SEM origem em git (nenhum .git, nenhuma home-page).

Este diretório é o resgate de emergência, não o lar definitivo: o produto merece repositório próprio (decisão do owner pendente). Instalação atual em produção: venv dedicado em `/opt/workflow-factory` com symlink `/usr/local/bin/workflow-factory`. Qualquer mudança daqui em diante nasce neste source e chega ao runtime por promoção com rito; editar o site-packages direto é regressão.
