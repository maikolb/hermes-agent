# Threat model

Status: living pre-release review, updated 18 July 2026

## Assets and trust boundaries

Assets include backend credentials, provider secrets reachable through Hermes, conversations, workspace files, approval authority and profile identity. Trust boundaries exist at Android intents, local storage, OS notifications/clipboard, network/TLS, Hermes authentication, rendered Markdown/HTML, file previews and third-party push infrastructure.

## Threats and controls

| Threat | Current or required control | State |
| --- | --- | --- |
| Backend impersonation/TLS downgrade | HTTPS by default; endpoint validation; no silent HTTPS→HTTP fallback | Implemented |
| Cleartext LAN token exposure | Explicit opt-in plus literal private-address allow-list; persistent warning | Implemented, warning UI needs strengthening |
| WebSocket credential leakage | Mint a fresh 30-second single-use Dashboard ticket for each upgrade; never place the session cookie in the upgrade or log WebSocket URLs | Implemented; deployment proxies should still redact query strings |
| Token theft at rest | AES-GCM key generated in Android Keystore; ciphertext only in private preferences; backups disabled | Implemented |
| Draft leakage at rest | App-private backend/profile/session-scoped DataStore, hashed preference keys, backup/device-transfer exclusion, bounded size, and backend-forget cleanup | Implemented baseline; rooted-device risk remains |
| Pending-message leakage, replay or cross-session confusion | Text-only entries use app-private hashed backend/profile/durable-session keys, bounded entry/text counts, backup exclusion and backend/session cleanup; drain requires the selected authenticated connected runtime, preserves FIFO, accepts only audited `prompt.submit` statuses, removes only after acceptance, and stops after four failures | Implemented for the active session; rooted-device risk remains, queued attachments and background cross-session drain are omitted |
| Unsafe slash command exposure | Catalogue is server-sourced but Android allow-lists supported built-ins, excludes terminal-only commands, bounds input/output, and only runs commands the user explicitly submits | Implemented baseline; server-side command policy remains authoritative |
| Backend OAuth interception/replay | PKCE S256, state/nonce, exact app link and single-use code | Blocked on native upstream flow |
| Provider-account OAuth confusion or token leakage | Accept only server-advertised provider IDs and flow types; keep session identity profile-scoped; open only credential-free HTTP(S) URLs; keep pasted codes in non-saveable UI state; bound code/session/expiry/poll intervals; never return provider tokens to Android; never execute external CLI commands locally; confirm only server-advertised disconnects | Implemented baseline; real paid-provider grants were not mutated during QA |
| Duplicate or confused billing mutation | Render only server capabilities; bound decimal amounts and require an explicit amount confirmation; serialize billing mutations with backend identity changes; persist a backend-scoped charge intent before send and its accepted charge ID before polling; restore the unconfirmed lock after process death; scope retries to the same amount and idempotency key; stop settlement polling after five minutes; treat transport, cancellation, session, scope, and spending-authority loss after charge acceptance as unconfirmed; open only safe HTTP(S) portal or verification URLs; never store payment details on Android | Implemented baseline; physical QA used fixtures and did not submit a real charge, auto-refill change, or subscription mutation |
| Malicious deep link | Treat route IDs as untrusted; require authenticated backend lookup; no credentials/actions in links | Required before link routing ships |
| Exported component/intent spoofing | Only launcher activity exported; explicit declarations; validate all inbound content | Implemented baseline |
| Malicious Android share payload | Accept only `ACTION_SEND`/`ACTION_SEND_MULTIPLE`; strip NULs and bound text; parse, deduplicate and bound only authority-bearing `content://` URIs; reject `file://`, HTTP and user-info; recheck scheme before attachment; never auto-send or persist the share payload | Implemented baseline; real provider grants, task recreation and malicious-provider device tests deferred |
| Camera capture URI leakage | Non-exported AndroidX `FileProvider` exposes only the dedicated app-cache camera directory, grants only the temporary capture URI to the system camera, and deletes captured content after the bounded attachment reader finishes | Implemented and exercised on the physical Samsung; a rooted device remains outside the app security boundary |
| Screenshot/recents leakage | Activity starts protected until the durable device-local preference resolves; optional secure-screen mode applies Android `FLAG_SECURE` to screenshots, screen recording and recent-app thumbnails across every Hermes surface | Implemented baseline; physical-device capture/recents verification deferred, and external cameras/root bypass remain out of scope |
| Clipboard leakage | Copy occurs only after an explicit labelled action on visible message text; sudo/secret values never enter message state; Android owns clipboard disclosure and auto-clear behavior | Implemented baseline; older Android versions may retain user-copied text until another clipboard write |
| Shared-file grant leakage | Only already-bounded preview bytes are copied into an app-private cache path; the non-exported provider grants a single read-only URI with its validated MIME type; a subsequent export prunes cache entries older than one hour | Implemented and exercised on the physical Samsung; a chosen recipient can retain content after the grant |
| Notification privacy | Secret content hidden by default; separate channels; action tokens | Blocked on push contract |
| Markdown/tool-output injection | Parse GFM into native Compose text, tables and rounded syntax-highlighted code; do not load remote images or execute raw HTML; open only bounded credential-free HTTP(S) links through Android; approval UI remains typed and separate | Implemented baseline; canonical inline media and specialised citation rendering remain incomplete |
| WebView compromise | HTML preview disables JavaScript, DOM storage, file/content access and mixed content; blocks navigation; intercepts all network requests; and injects a restrictive CSP | Implemented baseline; the WebView still shares the app process and needs an adversarial corpus test |
| Path traversal/file preview | Bound and NUL-check requested paths, require response-path identity, validate MIME and decoded size, isolate active HTML, and export through SAF or a single read-only provider grant | Implemented baseline; malicious-backend and unusual document-provider device tests remain |
| Oversized payload/zip bomb | HTTP/body, attachment, audio, decompression, pixel and page limits; stream to disk where supported | Partial; voice payloads are capped at 25 MiB before decode/playback |
| Microphone/audio residue | Request microphone only at use; app-private temporary files; bounded duration/size; delete on submit, cancel, navigation, focus interruption, failure, stop and completion | Implemented for voice slice; rooted-device/cache-forensics risk remains |
| Malicious spoken-audio response | Accept only bounded base64 `audio/*` data URLs from the authenticated Hermes backend; decode through Android media APIs; never execute or expose the server path | Implemented |
| Messaging credential leakage or cross-profile writes | Render only server-advertised profile fields; keep edits out of saved UI state and Android persistence; submit profile explicitly; show only server-redacted existing values; never log values | Implemented baseline |
| Accidental gateway disruption | Enable/disable and credential writes are separate from restart; restart names the selected profile and requires an impact confirmation; action polling is bounded | Implemented baseline |
| MCP credential disclosure or local execution confusion | Decode only non-secret server summaries; omit env/header maps, redact endpoint user-info/query data, keep catalog env values in non-saveable password state, accept only advertised env names, never run MCP/bootstrap commands on Android, and label probes/installs/reloads as Hermes-owned operations | Implemented baseline; rooted/process-memory risk remains while a credential field is open |
| Unsafe MCP activation or removal | Install only a server-advertised catalog identity after source/target/bootstrap review; reject remote OAuth entries; poll only the canonical background action name; toggle/remove only a server advertised by the selected profile; require exact REST acknowledgements and confirmed destructive actions; then invoke audited `reload.mcp` with `confirm=true` and preserve saved-but-reload-failed state | Implemented for catalog install/toggle/remove; custom add/edit and OAuth remain absent |
| Cross-profile or forged toolset mutation | Fetch the active profile, render only server-advertised toolset identities, profile-scope every read/write, send only the audited boolean, and require name/platform/state acknowledgement equality before updating UI | Implemented baseline; live disposable-backend and device checks deferred |
| Configuration secret disclosure, stale overwrite or policy downgrade | Intersect `/api/config` with `/api/config/schema` and an audited positive key set in memory; reject secret-like, unknown/object/list and high-risk policy/execution keys; never persist values; bind reads/writes to the observed active profile; send one bounded nested field through deep merge; require `ok=true` | Implemented baseline; the server schema has no sensitivity/risk annotations, and device/disposable-profile checks are deferred |
| Cross-session orchestration interference | Subagent interruption accepts only an ID currently advertised as active; background-process stop includes the open runtime session and Hermes verifies process ownership; both require a separate confirmation | Implemented baseline |
| Spawn-tree path injection or archive confusion | Request a bounded authenticated archive index; retain paths only in memory and never display them; load only an exact currently advertised path; rely on Hermes' independent spawn-tree-root confinement; require snapshot/list session identities to agree; bound and normalise every untrusted archived field | Implemented baseline; rooted memory inspection and compromised-backend risk remain |
| Accidental global delegation pause | UI states that active children continue, labels the switch by resulting action, changes only `delegation.pause`, and restores the observed server state on refresh | Implemented baseline |
| Malicious skill | Preserve Hermes review/scan boundary; show source/origin/trust; never direct-install around Hermes | Planned |
| Tool-output approval spoofing | Approval UI is driven only by a typed `approval.request` event bound to runtime session, never Markdown | Implemented |
| Checkpoint confusion or unintended rollback | Accept only full checkpoint hashes returned by `rollback.list` for the open runtime; require the matching `rollback.diff` preview, recheck it immediately before a separate restore confirmation is acted on, and cancel if it changed; refuse full restore during a run; omit `file_path`; reload authoritative history after success and clear stale local history if reload fails | Implemented for full workspace rollback; upstream has no atomic diff-version precondition, and disposable-workspace/device recovery QA is deferred |
| Sudo or secret prompt leakage | Typed gateway events open a blocking password-semantics field backed only by non-saveable Compose state; values are sent once through the matching response method and never enter timelines, drafts, preferences, logs or diagnostics | Implemented |
| Replay/duplicate events | Stable tool/request IDs; reducer upsert; authoritative rehydrate | Partial; stream cursor upstream gap |
| Cross-profile/session confusion | Cache/navigation identity includes backend, profile, durable ID and runtime ID; events filtered by runtime | Implemented baseline, more tests required |
| Diagnostic leakage | Export only an allowlisted, bounded report through an explicit SAF destination; flatten and bound attacker-controlled metadata; apply defense-in-depth redaction to endpoint, connection, capability and diagnostic text; never include cookies, passwords, conversations, provider settings or raw config | Implemented baseline; real document-provider, cancellation, storage-full and exported-file review deferred |
| Dependency compromise | Version catalogue, dependency locking, Dependabot/review, secret scan, release SBOM, checksum manifest, two-clean-build payload comparison, and GitHub provenance attestation | Partial: release-candidate CI covers locking, SBOM, checksums and reproducibility; secret scanning, dependency update automation, owner certificate and artifact review remain promotion gates |
| Android backup extraction | `allowBackup=false`, cloud/file exclusions | Implemented |

## Release gates

Before public release: complete MASVS-aligned review, dependency and secret scans, network interception tests, exported-component audit, WebView/file corpus tests, notification-action replay tests, database extraction test, diagnostic redaction fixtures, SBOM, signed provenance and an independent review of every high-risk finding. No critical or high finding may remain open.
