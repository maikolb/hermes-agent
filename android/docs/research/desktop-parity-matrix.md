# Desktop parity matrix

Baseline: Hermes Agent `5122ddd478143a6901bb752cf8ebcd1c5154b6da`, Agent `0.18.2`, Desktop `0.17.0`, verified 18 July 2026. Provider-account OAuth and billing were rechecked against upstream `5988fe6cd5547d3620df1de889ac6007f5463b4d` on the same date. Reconnect and command changes were rechecked against upstream `614dc194ea7d853d39f9e84582ec62156f41a475`, Agent `0.18.2`, Desktop `0.17.0`, on 19 July 2026. Dashboard authentication parity was rechecked against shipped Desktop source at upstream `f15a38ee73631b3cd5f7d30765c37d5f0245d403` on 7 August 2026; the oldest verified Hermes Agent version for that renewable auth path is `0.20.0`, with Desktop `0.17.0`. The parity frontier was re-audited against official Hermes Agent `main` at `b9aa9289a8083f2e9d248ad6837b2938f5ee92d7` on 8 August 2026. The oldest verified Hermes version for the original paths is `0.18.2`; live-turn reconnect projection and renewable multi-provider sessions require the cited current source contracts and degrade safely where explicitly documented. Version strings alone do not replace a commit pin. Status is intentionally strict: **implemented** means a real client path exists in this repository; **foundation** is tested domain/transport work without a complete surface; **blocked** identifies a concrete upstream contract gap; **not implemented** is not shipped as decorative UI.

| Desktop capability | Source / backend contract | Android status | Mobile adaptation | Test / limitation | Upstream change |
| --- | --- | --- | --- | --- | --- |
| Remote static-token backend | `electron/connection-config.ts`; `/api/status`, `/api/ws?token=` | Transport only; saved legacy records require reconnect | Dashboard-authenticated onboarding is the supported product path | Static-token gateway tests remain for protocol compatibility; no token onboarding UI | No |
| OAuth remote backend | `dashboard_auth/routes.py`; `/api/auth/ws-ticket` | Blocked | Native PKCE app link, revocable device session | Browser cookies cannot be transferred safely from Custom Tabs | Native OAuth exchange |
| Username/password dashboard auth | `/api/auth/providers`; `/auth/password-login`; renewable session cookies; `/api/auth/ws-ticket`; `/api/ws?ticket=` | Implemented for advertised password providers | Native provider selection and credential form, Keystore-backed access/rotating-refresh/provider-routing cookie bundle, transparent response-cookie persistence, fresh single-use ticket per WebSocket | Current fake-Dashboard contract covers exact provider selection, discovery refresh, access expiry/refresh rotation, REST and ticket cookie reuse, ticket-only WebSocket upgrade, legacy single-access-cookie parsing, and password non-persistence; physical refresh-rotation QA against current upstream remains pending | No |
| Multiple saved backends | Desktop connection config | Implemented baseline | Add/test/select/forget backends; metadata in DataStore and tokens in Keystore | HTTP+WebSocket validation; discovery/QR pairing pending | No |
| Capability/version display | `/api/status`; `session.info.desktop_contract` | Implemented baseline | Dedicated diagnostics surface plus contract warning and version-gated attachment/YOLO controls | Displays app, audited upstream, server, auth, connection, capabilities and session contract; canonical capability document remains desirable | Canonical capability document desirable |
| Session list across profiles | `/api/profiles/sessions` | Implemented | Phone atlas; tablet rail | Real REST path | No |
| Session resume | `session.resume` | Implemented | Durable→runtime identity translation | Reducer tests; real backend pending | No |
| New session | `session.create` | Implemented | One-tap new conversation | Real RPC path | No |
| Streamed text | `message.start/delta/complete` | Implemented | Stable assistant row, no per-token animation | Reducer test | Replay cursor for exact reconnect |
| Message rendering/actions | Desktop `assistant-message.tsx`, `markdown-text.tsx`, `copy-button.tsx`; Android clipboard/share APIs | Implemented baseline | Completed assistant replies render GFM headings, emphasis, lists, quotes, tables, inline code and rounded syntax-highlighted code fences; credential-free HTTP(S) links open through Android while other schemes remain inert. Completed messages retain partial selection, whole-message copy/share and Hermes read-aloud. Streaming follows only while the reader remains near the end and otherwise exposes an accessible jump-to-latest action | Physical Samsung Compose rendering/copy/action tests plus read-aloud QA and JVM link-boundary/scroll-policy tests; active streams use the stable lightweight renderer until completion, while specialised reference/media actions remain incomplete | No |
| Structured tool activity | `tool.start/progress/complete`; `message.interim`; structured history content parts | Implemented typed baseline | Ordered text/reasoning/tool/reference/artifact/media/error/blocking parts use stable advertised identity with explicit bounded fallbacks; Desktop interim commentary seals without overwriting the following final response | Reducer fixtures cover ordered history, media/source shapes, interim replay/idempotence, runtime isolation and stable tool IDs; specialised tool/media actions remain pending | No |
| Reasoning visibility | `reasoning.delta/available`; `display.show_reasoning` | Implemented | Collapsed disclosure plus the authoritative schema-driven Hermes visibility setting | Reducer coverage plus schema parse, boolean validation and nested-patch tests | No |
| Status and compression | `status.update` | Implemented | One rounded live lifecycle row replaces prior transient status, gives compaction/context pressure distinct treatment, and clears on Hermes `ready` without touching transcript messages | Reducer replacement/clear/data-preservation test plus full Android gate | No |
| Approval allow/deny | `approval.request/respond` | Implemented | Blocking prompt bound to source run | Reducer test; permanent-allow confirmation needs hardening | Push action token for background |
| Clarification | `clarify.request/respond` | Implemented | Choice/text blocking prompt | Real RPC path; accessibility UI test pending | Push for background |
| Sudo/secret request | `sudo.request/respond`, `secret.request/respond`, `sudo.expire`, `secret.expire` | Implemented | Blocking password-semantics prompt; value uses non-saveable Compose state, cancel sends the upstream empty response, and matching expiry removes only its own prompt | Reducer request/expiry tests plus two physical Samsung Compose instrumentation tests for masked semantics, unmodified submission and cancellation; no value enters timeline, draft, preferences or logs | No |
| Stop/interruption | `session.interrupt` | Implemented | Stop replaces send during active submission | Real RPC path | No |
| Steer/redirect | `session.steer` | Implemented | Composer switches to explicit steer while a run is active; stop remains separate | RPC path; real backend test pending | No |
| Retry/undo/compress/reset | `session.undo`, `session.close`, `session.create`, `prompt.submit` | Implemented | Confirmed retry/undo/reset actions; retry preserves the last authoritative user text, reset closes the old runtime before creating a clean one | Reducer and pinned result fixtures plus physical reset smoke; undo trims live UI state instead of reloading the pre-undo durable transcript | No |
| Branch | `session.branch` | Implemented baseline | Confirmed session action preserving the visible transcript | Branch identity fixture; real backend test pending | No |
| Session rename/archive/delete/search | `session.title`; `session.delete`; `/api/sessions/search` | Implemented | Confirmed destructive action; immediate recent-list filtering plus debounced profile-scoped FTS message search | Typed route/result tests and physical upstream search/delete smoke; remote search result deletion intentionally requires opening/closing the session first | No |
| Reconnect without duplicates | `session.resume`; `session.activate`; live `inflight`, `queued`, and `running` projection | Implemented for current upstream; exact missed-delta replay remains blocked | Same-session reconnect keeps the visible transcript, rehydrates the matching backend history, prefers the returned continuation after compression rotation, overlays authoritative accepted live and queued turns, retains stable blocking requests and running tool/reasoning state, and restores running state. Older servers preserve only the same-runtime local pending turn while they still report a live run | Pinned `614dc194` response fixture plus reducer, rotated-session, and concurrent-open tests cover authoritative projection, stale-local replacement, queued ordering, stable pending state, runtime isolation, continuation identity, and latest-navigation-wins ordering; physical interruption during an active projected turn remains pending; no universal stream sequence or replay cursor | Event replay v1 remains required for exact missed deltas and durable server-restorable prompts |
| Draft persistence/queue/history | Desktop `store/composer-input-history.ts`, `store/composer-queue.ts`; `prompt.submit`; gateway `_handle_busy_submit` | Draft persistence, composer history, and active-session text queue implemented | Debounced backend/profile/session-scoped DataStore draft; history is derived newest-first from authoritative user messages; the bounded queue is durable, editable and FIFO, drains only while the selected runtime is connected and idle, removes an entry only after Hermes accepts `streaming`, `queued` or `steered`, and stops after four failed attempts | Stable hashed draft/queue-key tests, deterministic history and queue reducer tests, and fake-gateway exact `prompt.submit` request/response coverage; queued attachments and Desktop's off-screen cross-session drain are intentionally absent pending durable attachment descriptors and Android background-delivery policy; device and real-backend checks deferred | No for active-session text queue; canonical durable attachment descriptor desirable |
| Slash autocomplete/catalogue | `commands.catalog`, `complete.slash`, `slash.exec`, `command.dispatch` | Implemented | Rounded inline palette with grouped catalogue, debounced completions and argument replacement | Typed catalogue/completion/dispatch fixtures; terminal-only commands are filtered, while backend skill and user quick commands remain discoverable | No |
| Model/provider catalogue | `model.options`; `config.set`; `/api/model/options`; `/api/env`; `/api/providers/validate`; `/api/providers/oauth` and its advertised start/submit/poll/cancel/disconnect routes | Dynamic catalogue, model selection, per-model reasoning/fast presets, API-key/custom-endpoint management, and provider-account sign-in implemented | Accounts and API Keys match Desktop's split when the account route exists; older servers retain the unaffected API Keys view. PKCE and device-code sign-in open only safe external web URLs, codes remain in transient UI state, polling is bounded by the server expiry, external flows expose server-advertised commands for copy only, and disconnect appears only when Hermes advertises it | Pinned upstream JSON plus exact cookie/profile/path/body REST fixtures and seven physical Samsung Compose tests cover connect, device code, PKCE submission, external handoff, confirmed disconnect, unknown-flow tolerance, older-server UI fallback, and negative cancellation acknowledgement. No paid-provider credential was mutated during QA | Canonical schema desirable |
| Reasoning effort/fast mode/YOLO | `config.get/set`, `session.info` | Implemented | Capability-gated effort/fast controls with per-model persistence; session-only YOLO with explicit warning | Protocol and durable-preset fixtures plus physical Compose instrumentation proving advertised reasoning/fast actions and confirmation before YOLO enablement | No |
| File/image/PDF attach | `file.attach`, `image.attach_bytes`, `pdf.attach`, `image.detach` | Implemented transient lifecycle | Native multi-document SAF and private camera `content://` sources use per-item validation, exact local-read progress, explicitly indeterminate one-frame upload, retry/cancel/remove, a 10 MiB cap, blocked executable MIME types, and backend/profile/runtime fencing. Ready handles are foreground-only and never enter saved state or the durable queue | Pure lifecycle/MIME policy tests plus pinned protocol fixtures and physical Samsung provider/read/delete instrumentation; final multi-item controls, rotation and real-provider proof remain pending. Large streaming is not claimed because the upstream RPC accepts one base64 frame | No for the bounded foreground path; durable server-owned attachment descriptors are needed for background queues |
| Android share target | Android `ACTION_SEND` / `ACTION_SEND_MULTIPLE`; existing `file.attach`, `image.attach_bytes`, `pdf.attach` | Implemented transient lifecycle | Bounded shared text and up to five distinct `content://` documents open a draft session, append independently to the composer, retain per-item failures for retry/removal, expose share-level Retry/Discard, reject all-failed document-only delivery, and never auto-send | Sanitizer/merge/lifecycle and repository tests cover text/URI/count/scheme/duplicate/readiness/all-failed bounds; real providers, grant expiry/revocation, cancellation, process recreation and layout still need final device proof | No for the bounded foreground path |
| File/artifact preview | `/api/files`, `/api/files/read`, `/api/files/download`; Desktop transcript-derived Artifacts page and generated-artifact rail | Managed workspace browser implemented; adaptive detected-artifact index/viewer implemented in source; canonical generated-artifact delivery pending | Profile-scoped search/filter, phone and expanded list/detail, origin chat/turn routing, bounded text/source/raster/PDF viewers, network-isolated HTML WebView, SAF export, and share/open-with through read-only MIME-preserving `content://` grants | Typed route, selected-profile request, path-boundary, MIME/data-limit, process-restoration, projection and provider/intent tests; current issue #23 runtime and grant-lifecycle proof pending | General remote artifact descriptor desirable |
| Voice STT/TTS | Desktop `hermes.ts`, `voice-playback.ts`, `speech-text.ts`; `/api/audio/transcribe`, `/api/audio/speak`; server `tools.tts_tool.text_to_speech_tool`; Android MediaSession | Implemented in source | Press-to-talk or tap-to-lock recording, slide up to lock, slide left to cancel, distinct reduced-feedback-aware transition haptics, live level/time feedback, per-reply read-aloud, exact MediaSession/notification pause-play-stop state, private metadata, focus recovery, cleanup, and Android system output selection | REST/payload/sanitisation plus media-state/haptic policy tests compile; physical Samsung permission, gesture, real Hermes TTS, lock-screen/Bluetooth controls, focus interruption, output-switcher, notification and cache-cleanup QA remain required | No |
| Profiles management | `/api/profiles`; `/api/profiles/active`; `/api/profiles/{name}/soul`; `/setup-command`; `/model`; profile-scoped `session.create`/`session.resume` | Implemented remote identity baseline | List/create/rename/delete, sticky default, current-process distinction, start profile-scoped session, bounded acknowledgement-checked SOUL editing, copy-only host setup guidance, and explicit provider/model assignment | Exact REST/body/acknowledgement and discard-policy tests; physical process-recreation/editor QA pending. SAF archive portability remains blocked because upstream import/export exchanges backend filesystem paths rather than archive bytes | Bounded byte-stream profile export/import plus explicit remote backend routing contract |
| Skills installed/hub/manage | REST skills/hub APIs, `skills.manage` | Installed list/search/toggle plus hub featured/search/preview/scan/install/update/remove implemented | Mandatory source review and security verdict before install; blocked scans cannot be overridden | Typed REST route tests; background actions use bounded redacted status polling | No |
| MCP catalogue/config/reload | `/api/mcp/servers`; `/api/mcp/catalog`; `/api/mcp/catalog/install`; `/api/actions/{name}/status`; `/api/mcp/servers/{name}/test`; `/enabled`; `DELETE /api/mcp/servers/{name}`; `reload.mcp` | Inspection, backend probes, reviewed non-OAuth catalog install, bounded background-install polling, confirmed removal, enable/disable and current-runtime reload implemented; custom add/edit, remote OAuth and per-tool filters absent | Profile-scoped searchable lists; install review exposes source/target/bootstrap and accepts only advertised env names in non-saveable password fields; Android never runs bootstrap locally, removes only an advertised server, and reloads only a matching live session/current profile | Typed REST/cookie fixtures cover list/catalog/probe/toggle/install/delete and canonical action identity; install safety tests cover required/unadvertised values and OAuth exclusion; reload fixture covers compute-host fields; cloud gate only, with device and disposable-backend checks deferred | Safe per-server patch and client-returned OAuth authorization flow required for edit/OAuth |
| Toolsets/providers/config | `/api/tools/toolsets`; `PUT /api/tools/toolsets/{name}`; `/api/config`; `/api/config/schema`; provider REST APIs | Dynamic profile-scoped toolsets and provider settings plus schema-driven general configuration implemented | Android intersects the schema with an audited positive set of boolean, number, select and text keys, writes one nested field through the server's deep merge, and keeps provider secrets and high-risk global bypass/network/execution relaxations in dedicated guarded surfaces | Typed cookie/profile/path/body/ack fixtures plus schema, positive-key, secret/high-risk-key, type, bound and nested-patch tests; config values remain process-memory only; running sessions may retain prior values; device/disposable-backend checks deferred | No for the safe schema-driven surface; a sensitivity/risk annotation in the formal schema would reduce client-owned exclusions |
| Cron/jobs/runs | REST cron routes | List/create/edit/delete/pause/resume/run-now and bounded run history implemented | Exact schedule editor labels server-side execution/timezone; run rows open their session | Typed REST route test; natural-language schedule helper pending | Push delivery for mobile result notifications |
| Messaging management | Desktop `app/messaging/index.tsx`; `/api/messaging/platforms`, platform update/test routes, `/api/gateway/restart` | Implemented baseline | Profile-scoped searchable platform catalogue, live status/error/home-channel detail, non-persistent Hermes-owned setup fields, enable/disable, confirmed removal, connection test, and separately confirmed gateway restart | Typed route tests plus physical Samsung catalogue/detail/password-semantics/test-result/restart-confirmation QA against the isolated pinned backend; WhatsApp and Telegram guided pairing flows remain server-specific follow-up work | No |
| Memory / learning / Starmap | `/api/learning/graph`; `/api/learning/node`; Desktop Starmap graph | Profile-scoped learning baseline implemented | Searchable native node and memory-card presentation, category/use/state/link metadata, bounded node detail editing, acknowledgement-checked updates, and confirmed skill archive or memory removal | Exact profile query/body/acknowledgement fixtures and search-policy tests; physical phone/tablet/process-recreation QA pending. Memory-provider and Curator endpoints remain current-process scoped and are not exposed as if they target an arbitrary selected profile | Explicit remote profile routing for Memory/Curator status and actions |
| Agents/Command Center | Desktop `app/agents/index.tsx`, `store/subagents.ts`; TUI `spawnHistoryStore.ts`; `delegation.status`, `delegation.pause`, `subagent.interrupt`, `process.list`, `process.kill`, `spawn_tree.list/load` | Implemented baseline including TUI-persisted replay | Rounded Command Center shows the live global delegation cap/pause state, active parent-child workstreams, streamed detail, matching child sessions, current-session processes, and a bounded cross-session archive list whose selected snapshot is defensively normalised without exposing the server filesystem path | Deterministic live-event/tree and archived camelCase-normalisation tests plus fake-gateway exact list/load params; only an advertised archive path can be loaded and Hermes independently confines it to the spawn-tree root; snapshots exist only for turns persisted by the TUI; cloud gate only, with device/disposable-backend checks deferred | Push beneficial; no direct subagent retry RPC; a server-owned all-client snapshot writer would broaden archive coverage |
| Usage/token accounting | `/api/analytics/usage`; `session.context_breakdown`; `session.info.usage` | Implemented baseline | Profile-scoped 7/30/90-day totals and accessible row-based model/daily/tool/skill summaries; an open runtime adds current context capacity and category shares without relying on colour | Typed REST/cookie fixtures cover full and nullable legacy totals; gateway breakdown fixtures cover categories, empty fallback and unknown fields; live context failure preserves profile analytics; cloud gate only, with device/real-backend checks deferred | No |
| Nous billing | Desktop `settings/billing`; `billing.state`, `subscription.state`, `billing.charge`, `billing.charge_status`, `billing.auto_reload`, `billing.step_up`; `billing.step_up.verification` | Implemented | Rounded native summary, account, purchase, auto-refill and usage cards use only server-advertised capabilities and portal URLs. Money mutations require explicit confirmation, use bounded decimal validation, persist backend-scoped intent and settlement identity across process death, serialize against backend changes, preserve an idempotency key only for a retryable same-amount send, poll settlement for at most five minutes, treat mid-poll scope/session loss as ambiguous, and open the server device verification in the system browser | Pinned upstream wire fixtures, durable pending-charge and policy/amount/origin tests, and eleven physical Samsung Compose tests cover summary, plan price, usage precedence and overage, confirmed and invalid purchase, refusal and older-server states, auto-refill reconciliation, unconfirmed-charge locking and recovery, and logged-out portal recovery. QA did not submit a charge, mutate auto-refill, or change a personal Nous subscription | No |
| Checkpoint/rollback | `rollback.list/diff/restore`; `tools/checkpoint_manager.py` | Implemented for full workspace rollback | Session actions list only full checkpoint hashes advertised by the open runtime, require a bounded raw-diff preview, re-check that diff immediately before mutation, reject restore while a run is active, and require a second explicit confirmation; successful restore reloads authoritative `session.history` because the gateway removes the affected last turn | Pinned result fixtures, identity/preview/busy/change-detection safety tests, and a fake-gateway request contract cover list/diff/restore/history; the upstream contract has no atomic preview-version precondition, and file-scoped restore is deliberately omitted until a safe server file selector is linked; cloud gate only, with device and disposable-workspace checks deferred | General diff/version precondition desirable |
| Logs/doctor/security audit/backup | `/api/logs`; `/api/hermes/update/check`; `/api/ops/backup`; `/api/ops/backup/download`; `/api/ops/doctor`; `/api/ops/security-audit`; `/api/actions/{name}/status`; Android SAF | Bounded redacted host log/status, doctor, security audit, support-report export, and exact-receipt host backup export implemented | Host-wide logs, update state, and backup are labelled separately from profile-scoped controls; update apply and backup restore are not exposed. Backup requires confirmation, matches the returned process ID through exit 0, and streams the ZIP directly to a user-selected SAF document with bounded size and partial-destination cleanup | Typed route, response-bound, exact-receipt, redactor and report-boundary tests; server action may continue after mobile polling is cancelled or times out; document-provider/device checks deferred | No for backup export; server-side cancellation would improve abandoned-action handling |
| Notifications/deep links | Desktop native notifications | Local Android surface implemented; background delivery blocked | Contextual Android 13 permission UX, private redacted completion/action/failure/cron channels, exact typed target links, and a privacy-safe New chat home-screen widget; no inline mutation actions without short-lived tokens | Robolectric channel/rendering policy, destination parser, and widget-manifest tests; physical denial/settings/tap proof and any producer remain pending | Device delivery v1 |
| Local Termux runtime | Upstream Termux support | Research only | Detect/link to explicit companion | No cross-app runtime API | Optional Termux companion contract |
| Phone/tablet/foldable layout | Desktop has responsive renderer | Implemented baseline | Master/detail below 840dp, two-pane above; edge-to-edge system-bar icon contrast follows each active skin background | Pure contrast policy and Android-test compilation pass; physical 360dp Samsung QA passed at 100% and 130% text, while light/dark bars and tablet/foldable hardware QA remain pending | No |
| Secure screen / biometric re-entry | Android `FLAG_SECURE`, platform `BiometricPrompt`, DataStore; client-owned preferences | Implemented in source | App Settings exposes independent secure-screen and opt-in re-entry controls. Re-entry with biometrics or the explicit device-credential recovery path gates app composition on a fresh process and after five background minutes, survives configuration changes without relocking, keeps the window secure while enabled, and keeps biometric material and protected input out of saved state | JVM persistence/monotonic-time policy tests and Compose settings compilation pass; enrollment invalidation, lockout, cancellation, process recreation, screenshots/recents and real Samsung prompt behavior still require physical-device proof | No |
| Built-in appearance skins | `apps/desktop/src/themes/presets.ts`; client-owned `/skin` | Implemented | Rounded two-column native picker uses the exact `nous`, `midnight`, `ember`, `mono`, `cyberpunk`, and `slate` source palettes; Nous remains the default, the choice is local and durable, and Android follows the system light or dark setting | Exact palette, light-synthesis, retired-value fallback and DataStore persistence tests; all six dark presets plus representative source and synthesised light variants inspected on the physical Samsung, including process-restart restoration | No |
| Keyboard/accessibility/reduced motion | Desktop design system | Foundation | Semantics, labelled actions, reversible container motion | Physical reduced-motion and enlarged-text checks passed; full TalkBack, switch-access, and hardware-keyboard audit pending | No |
